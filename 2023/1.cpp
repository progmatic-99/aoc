#include <iostream>
#include <string>

using namespace std;

int main(void) {
  while(getline(cin, line)) {
    int len = line.length();
    int start = 0, end = len - 1;
    while (start != )
  }
  return 0;
}
